# YOLO12-MoE-Dynamic 🚀
# 继承自 YOLO12，集成 Dynamic-C3k2, LSKA, 及 Optimized-MoE

nc: 80 
scales:
  n: [0.50, 0.25, 1024] 
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]

# YOLO12 优化版 Backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  
  # --- 消融点 1: 浅层使用 C3k2_Dynamic 替代原 C3k2 ---
  # 目的：验证在下采样初期进行通道特征调制的有效性
  - [-1, 2, C3k2_Dynamic, [256, False, 0.25]] # 2
  
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2_Dynamic, [512, False, 0.25]] # 4

  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  
  # --- 消融点 2: 使用 LSKA 替代原 A2C2f ---
  # 目的：对比大核卷积（LSKA）与区域注意力（A2C2f）在计算量与精度上的平衡
  - [-1, 4, C2f_LSKA, [512, True]] # 6
  
  # --- 消融点 3: 引入中层 MoE ---
  # 目的：测试在 P4 特征层引入专家混合带来的容量提升
  - [-1, 1, OptimizedMOEImproved, [512, 4, 2]] # 7

  - [-1, 1, Conv, [1024, 3, 2]] # 8-P5/32
  
  # --- 消融点 4: P5 深层使用 LSKA + 高容量 MoE ---
  # 目的：验证 8 专家在深层语义浓缩时的表达能力
  - [-1, 4, C2f_LSKA, [1024, True]] # 9
  - [-1, 1, OptimizedMOEImproved, [1024, 8, 2]] # 10

# YOLO12 优化版 Head
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 7], 1, Concat, [1]] # 连接 P4 MoE 后的特征 (注意索引变化)
  
  # --- 消融点 5: Head 全面动态化 ---
  # 目的：验证 C3k2_Dynamic 在特征融合后抑制噪声的能力
  - [-1, 2, C3k2_Dynamic, [512, False]] # 13 (原 A2C2f 替换)

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]] # 连接 P3
  - [-1, 2, C3k2_Dynamic, [256, False]] # 16

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # 连接 head P4
  - [-1, 2, C3k2_Dynamic, [512, False]] # 19

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]] # 连接 head P5 (MoE输出)
  - [-1, 2, C3k2_Dynamic, [1024, True]] # 22

  - [[16, 19, 22], 1, Detect, [nc]] # Detect(P3, P4, P5)